# 隐私说明

桌面歌词以本地处理为主，不包含账号系统、广告 SDK、定位功能或自建遥测服务。1.04 的可选翻译组件涉及第三方 SDK，具体数据处理见下文。

## 本地读取

应用通过 Android MediaSession 读取当前媒体会话公开的歌名、歌手、专辑、封面、播放状态、时长与进度，并通过系统音频接口读取媒体音量。这些信息用于悬浮窗显示与歌词同步。只有用户主动点击播放控制、歌词或拖动进度条时，应用才会向当前媒体会话发送相应控制指令。

应用不录制音频，不调用麦克风，不读取文件中的音乐内容，也不收集位置。

## 网络请求

为了匹配歌词和缺失的专辑封面，应用会把当前歌曲的歌名、歌手以及用于候选校验的专辑和时长信息作为搜索参数，直接请求以下公共服务：

- LRCLIB
- QQ 音乐
- 网易云音乐

当上述歌词源均未找到可靠候选时，应用可能请求 Apple 的 iTunes Search API，仅用于取得同一歌曲的本地化标题、歌手别名和目录元数据，再返回上述歌词源重新搜索。应用不会通过该接口下载或播放 Apple 音频。

设置页每天最多向 GitHub Releases API 静默检查一次新版本，也可由用户手动触发。请求仅包含应用版本及常规网络信息，不上传播放历史或本地配置；检查失败不会影响歌词功能。

请求不经过作者自建域名。相应平台及 GitHub 可能按照各自的隐私政策记录常规网络信息，例如 IP 地址与请求时间。

## 可选补充翻译（1.04）

补充翻译默认关闭。离线翻译使用 Google ML Kit，用户选择下载语言模型后可在本机翻译；模型下载会连接相应服务。语言包可以单独删除。离线模型可能经英语中转，结果仅供理解参考。

ML Kit 不会将输入歌词和离线译文发送给 Google，但可能连接 Google 获取模型、更新及兼容性信息，并发送 API 性能和使用指标。详见 [ML Kit 隐私说明](https://developers.google.com/ml-kit/terms)。

选择 API 翻译后，缺少平台译文的歌词文本会发送到用户当前选择的 HTTPS 服务地址。应用初始提供 DeepSeek、智谱 GLM 和 Gemini 的可编辑配置，但不包含任何可用 API Key；用户也可删除这些配置或添加其他兼容服务。费用及数据处理由相应服务决定。API Key 使用 Android Keystore 加密，并排除在系统备份之外；不会交给歌词 WebView，也不会写入日志。

补充译文保存在应用缓存中，可在设置页单独清理；不影响平台歌词缓存。

## 权限用途

- 通知使用权：访问播放器公开的 MediaSession，不读取通知正文。
- 悬浮窗：显示桌面歌词。
- 网络：搜索歌词和备用封面，并低频检查 GitHub 新版本。
- 前台服务：保持用户主动开启的悬浮窗。

卸载应用会删除其本地设置与歌词缓存。
