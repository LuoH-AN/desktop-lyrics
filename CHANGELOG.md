# 更新记录

## 1.07

- 展开与收起形态可分别设置歌词字号、歌词颜色和动态背景；设置页通过统一胶囊切换当前编辑目标，升级后默认继承原设置
- 展开与收起分别记忆窗口尺寸和位置；修复多次收放后收起窗口轻微变小、两行歌词退化为一行或窗口被推向屏幕左侧的问题
- 全屏歌词固定使用展开形态的字号、颜色和背景，退出后恢复收起形态自己的显示设置
- 歌词源管理和偏移记忆管理恢复白色普通操作文字，仅清除、删除、全部重置等危险操作保留红色
- 新增轻量更新检查：每天最多静默查询一次 GitHub Releases；有新版本时在使用说明内显示非强制提示，可查看更新、稍后处理或仅忽略当前版本
- 使用说明底部加入当前版本与手动检查入口；网络不可用时静默跳过，不影响悬浮歌词运行

## 1.06

- 收紧同歌手、近似时长的回退，避免中文歌名明显不同或同名不同歌手被误选；手动重匹配不再采用只凭歌手和搜索排名的标题回退
- 新增字号下方的歌词源管理：查看双击记录、预览歌词、搜索下一候选、选用历史版本、恢复初始结果和删除选择记忆
- 重匹配前保存原始结果，支持离线回退；管理操作会使尚未返回的旧重匹配请求失效
- 旧版缺少候选身份信息的选择需重新核对后应用；同一记录的逐字数据不会被临时普通歌词响应覆盖
- 匹配管理共用一份本地存储，取消数量、容量及过期限制，支持清除全部匹配缓存；按歌曲合并不同来源，并支持搜索歌名或歌手
- 使用说明新增歌词源气泡，统一显示与交互区管理按钮的字体、字号、高度和配色
- 重排设置首页：动态背景独立成卡片，歌词字号与偏移上下相邻，歌词源管理与偏移记忆管理同排展示；统一操作气泡尺寸，并重新设计无级调色弹窗
- 设置界面改用纯黑背景与 Apple Music 红强调色；开启悬浮窗、选中项、两条滑杆和运行状态采用红色，普通功能文字保持白灰，无级调色色值按当前真实歌词颜色显示
- 悬浮窗播放状态圆点改为红色，并保留轻量呼吸光效
- 修复关闭悬浮窗时，尚未结束的歌词搜索向已关闭线程池继续提交任务、导致设置界面进程崩溃的问题

## 1.05

- QQ 音乐接入 QRC 逐字歌词与官方翻译，并保留普通歌词接口回退；修复部分歌曲此前只能取得逐行歌词、翻译缺失或“逐字”实际只有标题的问题
- 多源匹配改为分层搜索与统一评分：并行查询 QQ 音乐、网易云、LRCLIB，依次尝试“歌名 + 歌手”、精简标题与标题回退，并结合歌手、时长、专辑、版本、跨文字别名、逐字覆盖率和翻译覆盖率选取候选
- 原文模式更偏向完整逐字歌词，双语/中文模式更偏向官方翻译覆盖率；允许从同一录音的其他来源补齐能力，但不会让残缺逐字歌词覆盖完整逐行歌词
- 加强无效内容识别，过滤纯歌名、制作信息、平台占位提示和明显残缺歌词；旧歌词缓存升级后会自动重新验证
- 新增来源级重新匹配：单击歌词源切换来源，双击仅重搜当前来源的下一候选并记住结果，长按恢复自动匹配；重搜失败或超时会保留当前歌词
- 手动匹配记忆按歌曲与歌词源区分，最多保留 32 项、单项限制 256 KiB，并在 14 天后自动过期，避免长期占用空间
- 为 QQ 音乐与网易云请求加入限流/服务繁忙冷却，减少连续失败时的无效重试；LRCLIB 增加受约束的标题回退以改善小众外语歌曲命中率
- 新增“中负载”动态背景，播放时将背景限制约 30 帧；逐字高亮、歌词焦点与滚动仍按屏幕刷新节奏运行
- 优化展开与横向布局中的播放控制、音量和歌词源胶囊，统一高度、间距与字体；所有横向滚动区域补充边缘渐隐，进度时间样式与歌手信息保持一致
- 收起形态的缩放与关闭按钮会在无操作约 2.5 秒后平滑淡出，点击悬浮窗即可重新显示，兼顾防误触与快捷操作
- 修复歌名及收起歌词周围的矩形渲染痕迹、进度条边缘异常光柱、状态圆点被透明边界裁切，以及歌词换句/切源时的轻微跳动
- 重新匹配提示与恢复菜单改为跟随专辑色的半透明圆角界面，补充淡入淡出动画，与悬浮窗整体视觉一致

## 1.04

- 正式 APK 仅保留现代手机通用的 64 位 ARM（arm64-v8a），移除模拟器与旧 32 位架构的重复翻译库，安装包体积由约 71.7 MiB 降至约 25 MiB
- 重构多源搜索与匹配：QQ 音乐、网易云音乐、LRCLIB 并行查询，综合歌名、歌手、专辑、时长、版本标记与结果顺序评分，并自动尝试多个候选
- 当直接搜索均失败时，可通过 iTunes 目录补充歌曲的本地化标题与歌手别名，再回到原歌词源搜索；不会下载或播放 Apple 音频
- 支持纯符号歌名，并自动排除“无歌词纯音乐”等平台占位文案，避免把错误提示当作歌词
- 同等匹配质量下优先官方翻译、逐字歌词和完整时间轴；修复切歌、切源时沿用上一首候选、空 LRCLIB 候选不隐藏及歌词短暂抽动的问题
- 无时间轴纯文本歌词会标注来源、全行高亮并按歌曲进度平滑向下滚动，收起形态也可按进度展示
- 新增可选离线机翻：Google ML Kit 语言包按需下载、删除和管理，安装包不预装语言模型
- 新增多套兼容 Chat Completions 的 API 配置，可编辑或删除内置的 DeepSeek、智谱 GLM、Gemini，也可继续添加自定义服务；API Key 加密保存在本机
- AI 补充翻译采用按服务自适应的分批预取、去重和本地缓存，优先翻译当前播放位置附近的缺失译文，减少请求次数与 Token 消耗
- 补充译文会区分平台翻译、离线机翻和 AI 翻译；切歌、切源和设置变更会使旧请求失效，不覆盖平台已有翻译或原时间轴
- 歌词偏移改为按歌曲和歌词源分别记忆，可在管理页查看、单独删除或全部重置
- 竖屏展开歌词按窗口高度、字号与当前句高度自适应上移聚焦；小窗口自动保留足够空间
- 双击缩放键进入独立沉浸式全屏歌词页，退出后恢复悬浮窗；全屏歌词可独立横竖翻转，悬浮窗仍保持单击收放、长按调节大小
- 修正横屏游戏隐藏系统栏后仍预留顶部空白的边界计算，并允许悬浮窗进入挖孔与状态栏区域

## 1.03

- 新增悬浮窗媒体控制：上一首、播放/暂停、下一首；按播放器公开的 MediaSession 能力自动启用或禁用
- 进度条支持直接拖动跳转，扩大透明触控高度但保持原有细圆润外观；松手后再提交跳转，减少连续控制造成的卡顿
- 展开态滑动浏览歌词后，可点击任意带时间轴的歌词跳转到对应位置，并自动恢复实时跟随
- 播放控制与音量、歌词源同处专辑下方的信息行；优先保证控制键完整，窄窗口下右侧胶囊自动首尾循环滚动
- 音量标签改为扬声器图标加百分比，移除输出设备卡片及不再需要的“附近设备”权限

- 新增白、蓝、黑、粉四种歌词颜色预设，以及覆盖完整 RGB 色域的无级调色器；设置会即时同步到标准与收起悬浮窗
- 网易云歌词现在可读取官方翻译与逐字时间轴；外语歌词支持原文、双语和中文三种显示方式
- 收起形态显示两行且选择双语时，第二行固定显示当前歌词翻译，不再显示下一句候选
- 收起双语形态的翻译过长时会按当前句剩余时间单向滚动；普通候选下一句继续保持静止
- 收起形态遇到逐字歌词时按当前演唱字平滑推进长句，当前字始终保持在可视区域；普通 LRC 继续使用整句定时滚动回退
- 为收起逐字跟随加入缓动与小幅进度回拨抑制，减少播放器 MediaSession 校时造成的文字抽动；真正拖动进度时仍会重新定位
- 修复悬浮窗手动拉至屏幕满宽后，切换标准/收起形态会被安全适配再次缩窄约 8dp 的问题；两种形态仍分别记忆自己的尺寸，不新增共享宽度状态
- 多源匹配继续以歌名、歌手准确度为最高优先级，同等匹配质量下优先选择带逐字或翻译数据的网易云结果
- 修复 Apple Music 罗马字元数据与音乐平台日文、韩文名称不一致时的漏配；仅对平台搜索首项启用跨文字回退，继续严格拦截普通同名误配
- 展开悬浮窗会显示当前歌词源；存在多个有效候选时，点击歌词源即可手动切换，方便纠正同名误匹配
- 修复展开态顶部拖动层覆盖歌词源标签、导致存在 `›` 候选提示却无法点击切换的问题
- 将歌词源标签的 WebView 绿色矩形点击高亮替换为圆角药丸按压反馈
- 多歌词源提示由向右箭头改为向下箭头，更直观地提示标签可点击切换
- 扩大展开态右上按钮的实际触控区，三个按钮采用对称的 6dp 间距；形态切换后的 650ms 内忽略关闭点击，减少连续补点造成的误关
- 收起态两个控制图标向右微移 3dp，减少与长歌词的视觉重叠
- 新增 ±5 秒歌词偏移调节，支持 0.1 秒步进和一键归零，播放进度本身不受影响

## 1.01

- 歌词字号下限由 75% 大幅扩展至 35%，收起窗最小高度继续随字号缩放
- 展开悬浮窗支持在固定外框内将全部内容旋转 90°，不改变手机系统方向；横向布局采用左侧播放信息、右侧歌词的 25% / 75% 分栏
- 标准与收起形态均会在屏幕旋转后自动适配边界
- 悬浮窗开启期间保持屏幕常亮，关闭后自动恢复系统息屏策略
- 歌词搜索改为按平台结果顺序选择首个有效候选，自动跳过无效歌曲 ID，并在必要时去除片头曲、主题曲等标题附注后重试
- 加强歌手一致性校验，歌手不符的同名歌曲会被淘汰，并自动在 LRCLIB、QQ 音乐和网易云之间选择更可靠的歌词
- 弱网容错提升：整轮歌词搜索最长等待 10 秒，正常网络下仍会在结果返回后立即显示

## 1.00

首个公开正式版。

[B站视频演示：可能是最精致的安卓桌面歌词软件｜Apple Music / Spotify 实时悬浮显示](https://www.bilibili.com/video/BV1jNu66eEkr/)

- 本地 MediaSession 实时读取与按帧歌词同步
- LRCLIB、QQ 音乐、网易云多源歌词和封面回退
- 可拖动、自由缩放的标准/收起悬浮窗
- 手动歌词浏览、惯性滚动与自动恢复实时跟随
- 字号调节及动态最小高度
- 透明、低负载、高负载动态背景
- 音量与蓝牙、有线、USB 输出设备显示
- 独立包名 `com.tcrrry.desktoplyrics`
